edge-tts
requests
google-api-python-client
google-auth-oauthlib

# faster-whisper gives TRUE per-word timestamps so captions are frame-accurate. WITHOUT it the
# pipeline falls back to LENGTH-BASED ESTIMATION, which drifts out of sync with the voice (the
# "captions off-timing" problem). Installing it is what makes captions actually match the audio.
faster-whisper
pytrends
Pillow>=10,<13
imageio-ffmpeg>=0.6,<1
tzdata
