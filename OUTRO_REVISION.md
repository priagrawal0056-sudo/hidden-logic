# Barcode ending revision — September 16, 2026

The closing follow line now stays inside the explanation: a newly rendered final
state connects the unchanged item code to the resolved illustrative $3.00 price.
The objects settle and a lookup travels down the arrow. No jar-shelf footage,
replayed stock clip or duplicate follow label is used. The original narration,
single-tier captions and seven-second mechanism demonstration are retained.

This is also the shared editor's closing behavior: a separate final follow sentence
uses the topic's resolved storyboard state. Diagram plans cannot add a second CTA
label. Stock-source uniqueness checks remain enabled.

The music is continuous across cuts. Short dips were present in the source music,
not caused by edit-point crossfades. Music-only level smoothing reduces those
variations; it does not process or flatten the narration.

Verified delivered file: `outputs/outro-revision/hidden-logic-barcode-final-callback.mp4`

- Duration: 23.925 seconds.
- Integrated loudness: -14.02 LUFS; true peak: -2.77 dBTP; LRA: 3.3 LU.
- Full decode and black-frame checks passed; contact sheet inspected.
- Three unique stock sources, one mechanism animation and a distinct animated callback.
- 71 tests pass; release scan and diff checks pass.
- SHA-256: `235664427d5a82ec3646b4d26dc8ccbd192b9ef8cd61ecd365d26ebbf5a903c4`.

The preceding delivered preview measures -14.01 LUFS locally. The reported -17.1
LUFS could not be reproduced on that exact file; the new export has a distinct
filename and hash to identify what was measured. This revision is unpublished.
