# ─── SECRETS: never commit these ───────────────────────────────
config.json              # has your plaintext keys - keep it LOCAL only
client_secret.json       # OAuth client secret
yt_token.pickle          # YouTube auth token
*.pickle
set_hidden_logic_env*.bat

# ─── RENDERED OUTPUT: huge, regenerated every run ──────────────
drafts/
output/
*.mp4
*.wav
*.mp3
__pycache__/
*.pyc

# ─── logs ──────────────────────────────────────────────────────
logs/
pipeline_log.txt
*.log

# NOTE: the state .json files (idea_bank.json, ab_log.json, etc.) are
# deliberately NOT ignored - they MUST be committed so the pipeline
# remembers its state across runs. The workflow commits them each run.
