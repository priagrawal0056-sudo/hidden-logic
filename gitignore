# ─── SECRETS: never let these reach GitHub ─────────────────────
client_secret.json
yt_token.pickle
*.pickle
config.local.json
set_hidden_logic_env.bat

# ─── BIG / regenerated every run: no point uploading ───────────
drafts/
output/
*.mp4
*.wav
*.mp3
__pycache__/
*.pyc
pipeline_log.txt
*.log

# NOTE: config.json is NOT ignored here on purpose - you're committing
# the KEY-LESS version (blank keys), which is safe. Your real keys live
# in GitHub Secrets, not in the file. The state .json files (idea_bank,
# ab_log, etc.) are also NOT ignored - they MUST be committed so the
# pipeline remembers what it made.
